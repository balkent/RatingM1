{% extends 'base.html.twig' %}

{% block title %}Subject{% endblock %}

{% block body %}
    <h1>Subject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subject.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ subject.libelle }}</td>
            </tr>
            <tr>
                <th>MaximumRating</th>
                <td>{{ subject.maximumRating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_subject_index') }}" class="btn btn-light">Retour à la liste</a>

    <a href="{{ path('app_subject_edit', {'id': subject.id}) }}">edit</a>

    {{ include('subject/_delete_form.html.twig') }}
{% endblock %}
