{% extends 'base.html.twig' %}

{% block title %}Answer{% endblock %}

{% block body %}
    <h1>Answer</h1>

    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
        <a class="btn btn-light" href="{{ path('app_answer_index') }}">Retour à la liste</a>
        <a class="btn btn-success" href="{{ path('app_answer_edit', {'id': answer.id}) }}">edit</a>
        {{ include('answer/_delete_form.html.twig') }}
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ answer.id }}</td>
            </tr>
            <tr>
                <th>Result</th>
                <td>{{ answer.result|nl2br  }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="list-group">
        {% for supplement in answer.supplements %}
            <li class="list-group-item">{{ supplement.libelle }}</li>
        {% endfor %}
    </ul>
{% endblock %}
