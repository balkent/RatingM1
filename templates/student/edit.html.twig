{% extends 'base.html.twig' %}

{% block title %}Edit Student{% endblock %}

{% block body %}
    <h1>Edit Student</h1>

    {{ include('student/_form.html.twig', {'button_label': 'Enrigistrer'}) }}

    <a href="{{ path('app_student_index') }}" class="submit" class="btn btn-light">Retour à la liste</a>

    {{ include('student/_delete_form.html.twig') }}
{% endblock %}
