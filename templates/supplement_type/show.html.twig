{% extends 'base.html.twig' %}

{% block title %}SupplementType{% endblock %}

{% block body %}
    <h1>SupplementType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ supplement_type.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ supplement_type.libelle }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ supplement_type.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_supplement_type_index') }}">back to list</a>

    <a href="{{ path('app_supplement_type_edit', {'id': supplement_type.id}) }}">edit</a>

    {{ include('supplement_type/_delete_form.html.twig') }}
{% endblock %}
