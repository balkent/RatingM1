{% extends 'base.html.twig' %}

{% block title %}Edit Supplement{% endblock %}

{% block body %}
    <h1>Edit Supplement</h1>

    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
        <a class="btn btn-light" href="{{ path('app_supplement_index') }}">Retour à la liste</a>
        {{ include('supplement/_delete_form.html.twig') }}
    </div>
    {{ include('supplement/_form.html.twig', {'button_label': 'Enrigistrer'}) }}
{% endblock %}
